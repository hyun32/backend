server:
  port: 9090
spring:
  profiles:
    include: oauth
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/Hit-Community
    username: root
    password: Yun970804*
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect=org:
          hibernate:
            dialect: org.hibernate.dialect.MySQL5InnoDBDialect
  session:
    store-type: jdbc
    jdbc:
      initialize-schema: always
